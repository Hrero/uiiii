{
  "name": "root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean",
    "build": "lerna run build --include-dependencies --stream",
    "git": "git add . && git commit -m '-' && git push origin master"
  },
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
